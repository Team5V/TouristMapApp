//Holds a html model with data in one object and on change of that model calls the view.update(model)

const modelModule = () => {
  const currentDataModule = () => {

  };

  return {getDataModule, setDataModule}
};